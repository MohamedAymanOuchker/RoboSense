# Normalize line endings to LF for all text files so the repo behaves
# identically on Linux/macOS/CI regardless of the contributor's OS.
* text=auto eol=lf

# Binary assets must never be normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.gif binary
